# API Mode: mock | local | staging | production
# - mock: Use local JSON mock data (no backend required)
# - local: Use local wrangler dev server (http://localhost:8787)
# - staging: Use staging API
# - production: Use production API
VITE_API_MODE=mock

# API Base URL (only used when not in mock mode)
VITE_API_BASE=https://api.bebond.net

# API Key for authenticated requests (optional for public endpoints)
VITE_API_KEY=

# Organization key for multi-tenant API
VITE_ORG_KEY=BB_vrconcierge
